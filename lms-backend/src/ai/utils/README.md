# Utils

## Purpose
Utility functions specifically scoped to AI operations.

## Responsibilities
- AI usage logging (tokens, latency, cost estimation).
- Text processing (e.g., stripping markdown from LLM JSON responses).
- Token counting estimation (if not handled by the provider SDK).

## Future Extension Points
- Advanced prompt template compilation logic.
